/*
Retorna um novo array com todos os elemenos de um sub-array
concatenados de forma recursiva de acordo com a profundidade
especificada (depth)
 */  

const idades = [10, 20, [30, 40, [60, 70]]];

console.log(idades);
console.log(idades.flat(2));
 
